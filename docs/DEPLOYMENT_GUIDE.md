# 카페24 웹호스팅 배포 가이드

## 📋 사전 준비사항

1. 카페24 호스팅 계정 및 도메인
2. 데이터베이스 생성 (MySQL/MariaDB)
3. FTP 접속 정보
4. 도메인 SSL 인증서 (HTTPS)

---

## 🗄️ 1단계: 데이터베이스 설정

### 1.1 데이터베이스 생성
1. 카페24 호스팅 관리자 > 데이터베이스 관리 접속
2. MySQL/MariaDB 데이터베이스 생성
3. 데이터베이스 이름, 사용자명, 비밀번호 확인

### 1.2 데이터베이스 스키마 생성
1. phpMyAdmin 접속
2. 생성한 데이터베이스 선택
3. `database/schema.sql` 파일 내용 실행
4. 필요한 마이그레이션 파일들 실행

---

## ⚙️ 2단계: 환경 설정

### 2.1 호스팅 설정 파일 생성
1. `config/hosting.php.example` 파일을 복사하여 `config/hosting.php` 생성
2. `config/hosting.php` 파일을 열고 다음 정보를 수정하세요:

```php
// 데이터베이스 설정
define('DB_HOST', 'localhost'); // 카페24 DB 호스트
define('DB_NAME', 'your_database_name'); // 실제 DB 이름
define('DB_USER', 'your_database_user'); // 실제 DB 사용자명
define('DB_PASS', 'your_database_password'); // 실제 DB 비밀번호

// Base URL (실제 도메인으로 변경)
define('BASE_URL', 'https://your-domain.com');

// API JWT 시크릿 키 (강력한 랜덤 문자열로 변경)
define('API_JWT_SECRET', 'CHANGE-THIS-TO-STRONG-RANDOM-SECRET-KEY');

// 토스페이먼츠 실제 키로 변경
define('TOSS_CLIENT_KEY', 'your_toss_client_key');
define('TOSS_SECRET_KEY', 'your_toss_secret_key');
```

### 2.2 Vue.js 앱 환경 변수 설정
`manager-app/.env.production` 파일을 열고 수정:

```env
VITE_API_BASE=https://your-domain.com
```

---

## 📦 3단계: 파일 업로드

### 3.1 FTP 접속
- 호스트: 카페24 FTP 서버 주소
- 포트: 21 (또는 지정된 포트)
- 사용자명/비밀번호: 호스팅 관리자에서 확인

### 3.2 업로드할 파일
카페24 호스팅의 일반적인 디렉토리 구조:
- `www/` 또는 `public_html/` - 웹 루트 디렉토리

**업로드할 파일/폴더:**
```
✅ 모든 PHP 파일
✅ assets/ 폴더
✅ components/ 폴더
✅ config/ 폴더
✅ database/ 폴더 (connect.php, migrations/)
✅ includes/ 폴더
✅ pages/ 폴더
✅ api/ 폴더
✅ index.php
✅ router.php
✅ .htaccess
✅ manager-app/dist/ (빌드된 Vue.js 앱)
```

**업로드하지 않을 파일:**
```
❌ node_modules/
❌ .git/
❌ manager-app/src/ (소스 파일, dist만 업로드)
❌ manager-app/android/
❌ tosspayments/ (샘플 파일, 필요시만)
❌ storage/sessions/*.sess_* (세션 파일)
```

---

## 🔨 4단계: Vue.js 앱 빌드

### 4.1 로컬에서 빌드
```powershell
cd manager-app
npm install
npm run build
```

### 4.2 빌드된 파일 확인
빌드 완료 후 `manager-app/dist/` 폴더가 생성됩니다.

### 4.3 빌드된 파일 업로드
`manager-app/dist/` 폴더의 모든 파일을 호스팅의 `manager-app/` 디렉토리에 업로드합니다.

---

## 📁 5단계: 디렉토리 구조

카페24 호스팅에서 권장하는 구조:

```
www/ (또는 public_html/)
├── index.php
├── router.php
├── .htaccess
├── config/
│   ├── app.php
│   └── hosting.php (수정 필요)
├── api/
├── pages/
├── components/
├── assets/
├── database/
├── includes/
├── manager-app/
│   └── dist/ (빌드된 Vue.js 앱)
│       ├── index.html
│       ├── assets/
│       └── ...
└── storage/
    └── sessions/ (쓰기 권한 필요)
```

---

## 🔧 6단계: .htaccess 설정

루트 디렉토리에 `.htaccess` 파일이 있는지 확인하고, 없으면 생성:

```apache
# PHP 버전 설정 (카페24 권장 버전 사용)
# AddHandler application/x-httpd-php74 .php

# URL 리라이팅
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    
    # HTTPS 강제 리다이렉트 (SSL 인증서가 있는 경우)
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
    
    # index.php를 통한 라우팅
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php?route=$1 [L,QSA]
</IfModule>

# 보안 설정
<FilesMatch "\.(env|log|ini)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# PHP 설정
php_value upload_max_filesize 10M
php_value post_max_size 10M
php_value max_execution_time 300
php_value memory_limit 256M
```

---

## 🔐 7단계: 파일 권한 설정

FTP 클라이언트에서 다음 디렉토리의 쓰기 권한 설정:
- `storage/sessions/` → 755 또는 777
- `storage/uploads/` (있는 경우) → 755 또는 777

---

## ✅ 8단계: 테스트

### 8.1 웹사이트 접속 확인
- `https://your-domain.com` 접속
- 메인 페이지가 정상적으로 표시되는지 확인

### 8.2 데이터베이스 연결 확인
- 로그인 페이지 접속
- 데이터베이스 연결 오류가 없는지 확인

### 8.3 API 테스트
- `https://your-domain.com/api/manager/me` 접속
- 인증 오류가 정상적으로 표시되는지 확인

### 8.4 Vue.js 앱 테스트
- `https://your-domain.com/manager-app/` 접속
- 매니저 앱이 정상적으로 로드되는지 확인

---

## 🚨 문제 해결

### 데이터베이스 연결 오류
- `config/hosting.php`의 DB 정보 확인
- 카페24 호스팅 관리자에서 DB 정보 재확인
- DB 호스트가 `localhost`가 아닐 수 있음 (호스팅 관리자 확인)

### 404 Not Found 오류
- `.htaccess` 파일이 업로드되었는지 확인
- `mod_rewrite` 모듈이 활성화되어 있는지 확인 (카페24 고객센터 문의)

### Vue.js 앱이 로드되지 않음
- `manager-app/dist/` 폴더가 올바르게 업로드되었는지 확인
- 브라우저 개발자 도구에서 콘솔 오류 확인
- `VITE_API_BASE` 환경 변수가 올바른지 확인

### CORS 오류
- `config/hosting.php`의 `API_CORS_ORIGINS` 설정 확인
- 실제 도메인으로 변경했는지 확인

---

## 📝 체크리스트

배포 전 확인사항:

- [ ] 데이터베이스 생성 및 스키마 적용 완료
- [ ] `config/hosting.php` 파일 생성 및 수정 완료 (hosting.php.example 복사)
- [ ] `manager-app/.env.production` 파일 수정 완료
- [ ] Vue.js 앱 빌드 완료 (`npm run build`)
- [ ] 모든 파일 FTP 업로드 완료
- [ ] `.htaccess` 파일 업로드 완료
- [ ] 파일 권한 설정 완료
- [ ] SSL 인증서 적용 완료 (HTTPS)
- [ ] 웹사이트 접속 테스트 완료
- [ ] 로그인 테스트 완료
- [ ] API 테스트 완료
- [ ] Vue.js 앱 테스트 완료

---

## 📞 지원

문제가 발생하면:
1. 카페24 호스팅 관리자 > 고객센터 문의
2. 브라우저 개발자 도구에서 오류 확인
3. PHP 에러 로그 확인 (호스팅 관리자에서 확인 가능)
