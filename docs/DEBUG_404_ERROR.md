# 404 오류 디버깅 가이드

## 🔍 어떤 파일이 404인지 확인하기

### 1. 브라우저 개발자 도구 열기
- F12 키 누르기
- 또는 우클릭 → "검사" 선택

### 2. Network 탭 확인
1. 개발자 도구에서 **Network** 탭 클릭
2. 페이지 새로고침 (F5 또는 Ctrl+R)
3. 빨간색으로 표시된 파일 확인 (404 오류)

### 3. 일반적인 404 원인

#### A. JavaScript 파일 404
**오류 예시**: `Failed to load resource: /manager-app/assets/index-xxxxx.js`

**원인**:
- `assets/` 폴더가 업로드되지 않음
- 파일명이 다름 (빌드마다 파일명이 변경됨)

**해결**:
- `manager-app/dist/assets/` 폴더 전체를 호스팅의 `/manager-app/assets/`에 업로드
- 빌드된 `index.html`의 파일명과 실제 파일명이 일치하는지 확인

#### B. CSS 파일 404
**오류 예시**: `Failed to load resource: /manager-app/assets/index-xxxxx.css`

**해결**: JavaScript와 동일하게 `assets/` 폴더 확인

#### C. manifest.webmanifest 404
**오류 예시**: `Failed to load resource: /manager-app/manifest.webmanifest`

**해결**:
- `manager-app/dist/manifest.webmanifest` 파일을 호스팅에 업로드

#### D. Service Worker 파일 404
**오류 예시**: `Failed to load resource: /manager-app/registerSW.js` 또는 `/manager-app/sw.js`

**해결**:
- `manager-app/dist/registerSW.js` 업로드
- `manager-app/dist/sw.js` 업로드

## 📋 필수 파일 체크리스트

호스팅 서버의 `/manager-app/` 폴더에 다음 파일들이 있는지 확인:

```
/manager-app/
├── index.html                    ← 필수!
├── .htaccess                     ← 필수!
├── manifest.webmanifest          ← 필수!
├── registerSW.js                ← 필수!
├── sw.js                         ← 필수!
├── workbox-xxxxx.js             ← 있으면 업로드
└── assets/
    ├── index-xxxxx.js           ← 필수!
    ├── index-xxxxx.css          ← 필수!
    ├── vue-vendor-xxxxx.js      ← 필수!
    └── ... (기타 컴포넌트 파일들)
```

## 🔧 빠른 해결 방법

### 방법 1: 전체 dist 폴더 업로드

1. 로컬에서 `manager-app/dist/` 폴더의 **모든 파일** 선택
2. 호스팅 서버의 `/manager-app/` 폴더에 업로드
3. `.htaccess` 파일도 함께 업로드 확인

### 방법 2: 파일명 확인

빌드된 `index.html` 파일을 열어서 실제 파일명 확인:
- 18번째 줄: `<script src="/manager-app/assets/index-XXXXX.js">`
- 실제 파일명과 일치하는지 확인

## 🚨 여전히 안 되면

1. **브라우저 콘솔 확인**
   - F12 → Console 탭
   - 정확한 오류 메시지 확인

2. **Network 탭에서 실패한 파일 확인**
   - 어떤 파일이 404인지 정확히 확인
   - 파일 경로 확인

3. **호스팅 서버 파일 확인**
   - FTP로 접속하여 실제 파일이 있는지 확인
   - 파일명이 정확한지 확인

4. **브라우저 캐시 지우기**
   - Ctrl + Shift + Delete
   - 또는 개발자 도구 → Network 탭 → "Disable cache" 체크
